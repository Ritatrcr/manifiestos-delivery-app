global:
  host: prod.167.172.14.0.nip.io
  pathPrefix: "/"

app:
  image:
    tag: v1.0.7
  replicas: 3
  resources:
    requests: { cpu: "200m", memory: "256Mi" }
    limits:   { cpu: "500m", memory: "512Mi" }
  hpa:
    enabled: true
    minReplicas: 3
    maxReplicas: 8
    targetCPU: 70

postgresql:
  primary:
    persistence:
      size: 20Gi
